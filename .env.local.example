# Copy this file to `.env.local` and fill in real values.
# Do NOT commit `.env.local` to git.

# --- Firebase web app config (Next.js) ---
# Get these from: Firebase Console → Project Settings → General → SDK setup & configuration.
NEXT_PUBLIC_FIREBASE_API_KEY=your_web_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# --- Optional: backend-only Firebase service account (NOT used in browser) ---
# These are only needed if you use firebase-admin somewhere.
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# --- Test / CI settings ---
FIREBASE_SERVICE_ACCOUNT_JSON=your_service_account_json_here
# Enable test auth endpoints for e2e tests (do NOT enable in production)
ENABLE_TEST_AUTH=false

# --- Google / Gemini API (used by Python DSPy backend) ---
GOOGLE_GENAI_API_KEY=your_genai_api_key_here
# Alternatively you can use:
# GOOGLE_API_KEY=your_google_api_key_here
# GEMINI_MODEL_NAME=gemini-1.5-flash
# GEMINI_TEMPERATURE=0.3

# --- Python Backend Settings (src/ai/dspy/api.py) ---
ENVIRONMENT=local
PORT=8001

# Must match your Firebase project id AND the `.firebaserc` "default" project.
FIREBASE_PROJECT_ID=your-project-id

# --- Local Emulator Hosts (keep these if you use the emulators on default ports) ---
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
FIREBASE_STORAGE_EMULATOR_HOST=127.0.0.1:9199
