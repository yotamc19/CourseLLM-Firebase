# Copy this file to `.env.local` and fill in the values. Do NOT commit your real .env.local to git.
# Example for Firebase + GitHub OAuth settings used by this project (Next.js expects NEXT_PUBLIC_ prefix for client-side values).

# Firebase web app config (from Firebase Console -> Project Settings -> SDK setup)
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Optional: backend-only values (if you ever use server-side Firebase admin or Cloud Functions)
# These should NOT be exposed client-side. Keep them in a server-only env.
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com


# Notes:
# - Copy this file to .env.local (gitignored) and fill real values.
# - For local Next dev the project runs on port 9002 by default (see package.json). Add http://localhost:9002 to Firebase/GitHub authorized domains/redirect URIs.

# To run tests, set the following, see instructions
FIREBASE_SERVICE_ACCOUNT_JSON=your_service_account_json_here
# Enable test auth endpoints for e2e tests (do NOT enable in production)
ENABLE_TEST_AUTH=false

GOOGLE_API_KEY=your_key_here